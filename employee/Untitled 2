클래스:
1. java 클래스의 종류
	1. 메인클래스 : main() 포함
						프로젝트내에 단 하나만 존재해야 한다.
						프로젝트가 실행될 때, 가장 먼저 자동 호출되는 클래스	

	2. 일반 클래스 : 생성자 포함
						다른 클래스가 일반 클래스를 인스턴스화 해야만 사용 가능.
						즉, 하나의 객체로 생성해 주어야 한다!!!
						
	3. 추상클래스
	4. interface 클래스
	
일반 클래스의 구성요소
1. 클래스 선언부(필수)
  	public class 클래스명 {
  	
  	}
  	
2. 클래스 블록 내에는 반드시 생성자가 포함되어 있어야 한다.(필수)
	생성자
	public 클래스명과 동일(){
	}
	
3. 변수(다양한 타입의 변수들.../배열)
	int /float/String

4. 메서드들...
	public 반환타입 메서드명(){
	
	}
	
	public 반환타입 메서드명(변수선언){
	
	}
	변수선언 : 외부로부터 데이터를 전달받아 저장하는 매개체 역할
				 매개변수
				 
	//반환 데이터가 없을 경우
	 public void 반환타입 메서드명(){
	
	}
	
		//반환 데이터가 있을 경우(정수)
	 public int 반환타입 메서드명(){
		return 반환되는 정수
	}
	
			//반환 데이터가 없고, 외부로부터 전달받을 값이 있을 경우(정수)
	 public void 반환타입 메서드명(int n){
	
	}
	
				//반환 데이터가 있고(문자열), 외부로부터 전달받을 값이 있을 경우(정수)
	 public String 메서드명(int n){
		return 문자열;
	}
	
	//반환 데이터가 있고(문자열), 외부로부터 전달받을 값이 있을 경우(정수, 문자)
	 public String 메서드명(int n, char){
		return 문자열;
	}
	
	**반환되는 데이터는 반드시 하나의 데이터만 가능..!!!!!
	public String, String 메서드명(){		// 불가능(아래처럼 가능)
		return 문자열, 문자열;
	}

	public String[] 메서드명(){		// 가능
		String[] str={문자열,문자열};
		return str
	}

	* 일반 클래스 명 : EMPDataClass
	 
	 prj->실행->main()->클래스찾기->main()메서드 호출
	 ->new EMPDataClass();
	 1. EMPDataClass 파일 확인
	 2. EMPDataClass 클래스명 확인
	 2-1. 변수선언
	 3. 생성자 선언 , 메서드선언
	 4. 생성자 호출
